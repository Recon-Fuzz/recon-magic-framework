# Initialize Parallel Environments

## Run 

CREATE new directory `trees/`

> Execute these steps in parallel for concurrency
> Use absolute paths for all commands

CREATE first worktree: 
- RUN `git worktree add ./trees/setup-1 -b setup-1`

CREATE second worktree: 
- RUN `git worktree add ./trees/setup-2 -b setup-2`

CREATE second worktree: 
- RUN `git worktree add ./trees/setup-3 -b setup-3`